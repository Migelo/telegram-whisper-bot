use nix
layout python

# Environment variables for the Telegram Whisper project

# Replace "your_api_id_here" with your actual Telegram API ID.
export TELEGRAM_API_ID="YOUR_TELEGRAM_API_ID"

# Replace "your_api_hash_here" with your actual Telegram API Hash.
export TELEGRAM_API_HASH="YOUR_TELEGRAM_API_HASH"

# Specify the Whisper model to use (e.g., base, small, medium, large).
export WHISPER_MODEL="base"

# Replace "your_bot_token_here" with your actual Telegram Bot Token.
export TELEGRAM_BOT_TOKEN="YOUR_TELEGRAM_BOT_TOKEN"
